<Window x:Class="PeselBmiWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PeselBmiWpf"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Height="320" Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25*"/>
            <ColumnDefinition Width="75*"/>
        </Grid.ColumnDefinitions>

        <UniformGrid Rows="7" DataContext="" Margin="10 0 0 10">
            <Label Content="Dodaj nową osobę:" FontSize="18" FontWeight="Bold" Padding="0 8 0 0"/>

            <!--FirstName-->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="35*"/>
                    <ColumnDefinition Width="65*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Imię" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="15"/>
                <TextBox x:Name="FirstNameTextBox" Grid.Column="1" VerticalAlignment="Center" Background="LightGray" Padding="0 4 0 4" FontSize="13"/>
            </Grid>

            <!--LastName-->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="35*"/>
                    <ColumnDefinition Width="65*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Nazwisko" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="15"/>
                <TextBox x:Name="LastNameTextBox" Grid.Column="1" VerticalAlignment="Center" Background="LightGray" Padding="0 4 0 4" FontSize="13"/>
            </Grid>

            <!--Pesel-->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="35*"/>
                    <ColumnDefinition Width="65*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Pesel" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="15"/>
                <TextBox x:Name="PeselTextBox" Grid.Column="1" VerticalAlignment="Center" Background="LightGray" Padding="0 4 0 4" FontSize="13"/>
            </Grid>

            <!--Height-->
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="35*"/>
                    <ColumnDefinition Width="65*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Wzrost (cm)" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="15"/>
                <TextBox x:Name="HeightTextBox" Grid.Column="1" VerticalAlignment="Center" Background="LightGray" Padding="0 4 0 4" FontSize="13"/>
            </Grid>

            <!--Weight-->
            <Grid Grid.Row="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="35*"/>
                    <ColumnDefinition Width="65*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Waga (kg)" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="15"/>
                <TextBox x:Name="WeightTextBox" Grid.Column="1" VerticalAlignment="Center" Background="LightGray" Padding="0 4 0 4" FontSize="13"/>
            </Grid>

            <!--Button-->
            <Button Grid.Row="7" x:Name="AddPersonButton" Click="AddPersonButton_Click" Content="Dodaj Osobę" FontSize="12" Margin="0 5 0 5"/>
        </UniformGrid>

        <Grid Grid.Column="1" Margin="0 0 0 0">
            <DataGrid Name="DataGridPerson"  ItemsSource="{Binding People}" 
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              CanUserDeleteRows="True"
              Margin="8"
              ColumnWidth="*"
              PreviewKeyDown="DataGrid_PreviewKeyDown">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Imię" Binding="{Binding FirstName}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Nazwisko" Binding="{Binding LastName}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Pesel" Binding="{Binding Pesel}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Data urodzenia" Binding="{Binding BirthDate, StringFormat=dd.MM.yyyy}" IsReadOnly="True" Width="1.1*"/>
                    <DataGridTextColumn Header="Płeć" Binding="{Binding Sex}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Wiek" Binding="{Binding Age}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Wzrost (cm)" Binding="{Binding Height}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Waga (kg)" Binding="{Binding Weight}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="BMI" Binding="{Binding Bmi}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Kategoria" Binding="{Binding BmiCategory}" IsReadOnly="True" Width="1.3*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>
